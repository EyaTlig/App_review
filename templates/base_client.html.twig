<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Client - App Review{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        body {
            min-height: 100vh;
            padding-top: 70px; /* Hauteur approximative de la navbar */
        }
        .navbar a { color: #fff; }
        .card { border-radius: 10px; }
        .nav-logo {
            height: 50px; /* augmente si tu veux plus grand : 50px, 60px... */
        }

        .navbar-fixed {
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
        }
        #search-input {
            background-color: rgba(255, 255, 255, 0.2); /* gris transparent */
            color: #fff; /* texte blanc */
            border: none;
            border-radius: 50px; /* arrondi */
        }

        #search-input::placeholder {
            color: rgba(255, 255, 255, 0.7); /* placeholder semi-transparent */
        }

        #search-input:focus {
            background-color: rgba(255, 255, 255, 0.3);
            box-shadow: none;
            outline: none;
        }
    </style>
</head>
<body>
<!-- Navbar simple -->

<nav class="navbar navbar-expand-lg navbar-dark bg-dark navbar-fixed">
    <div class="container-fluid">
        <a class="navbar-brand d-flex align-items-center fw-bold" href="/">
            <img src="{{ asset('images/logo.png') }}" class="nav-logo me-2" alt="Logo">
            App-Review
        </a>

        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <form class="d-flex w-50 position-relative" id="search-form">
                <input class="form-control ps-5" type="search" name="search" id="search-input"
                       placeholder="Rechercher un commerce ou une catégorie" aria-label="Search">
                <i class="bi bi-search position-absolute" style="left: 15px; top: 50%; transform: translateY(-50%); color: rgba(255,255,255,0.7);"></i>
            </form>
        </div>

        <button class="navbar-toggler d-md-none" type="button" onclick="toggleSidebar()">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="d-flex ms-auto">

            {% if app.user %}
                <span class="navbar-text me-3">
        Bonjour, {{ app.user.name }}
    </span>
                <a class="btn btn-outline-light btn-sm" href="{{ path('app_logout') }}">Déconnexion</a>
            {% else %}
                <a class="btn btn-outline-light btn-sm" href="{{ path('app_login') }}">Connexion</a>
            {% endif %}
        </div>

    </div>
</nav>
<div class="container">
    {% block body %}{% endblock %}
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
{% block javascripts %}{% endblock %}
</body>
</html>
